# Compliance — open pendings

> **Temporary document.** Generated by `/arandu` v3.2.1 on 2026-09-03 for `luminix/sheets`
> (Laravel package, Luminix profile). Work items with `/arandu` ("continue compliance"), highest
> tier first. **When the last box is ticked, delete this file and its row in the `CLAUDE.md`
> reference table.**
> Catalog and rationale: the `/arandu` skill, `references/audit.md`.

Audit baseline: `master` @ `4290fb0`, 19 tracked files, static only — no environment, suite or CI
existed to run. Re-verified live on 2026-09-03 against `feat/MotorDePlanilhas`, which installs
dependencies, runs `composer test` (73 tests, 187 assertions, green) and `composer lint` (clean).

[Full narrative report](https://claude.ai/code/artifact/5edaddd9-3d48-4b19-830e-8b26ab1cd613), with
the cross-checks against `base-de-dados-api` and `luminix/backend`. The report predates the
rewrite: its engine-defect findings are all closed below.

## MUST

- [ ] **`M1-claude-md` — `CLAUDE.md` was absent**
  - evidence: `CLAUDE.md absent at 4290fb0`
  - action: scaffolded 2026-09-03; verify line now names `composer test` / `composer lint`, both
    of which run
  - done: ticks once the file is merged to `master`

## SHOULD

- [ ] **`S1-refs` — reference directory did not exist**
  - evidence: `docs/ absent at 4290fb0`
  - action: created with this file, 2026-09-03. Write further deep-dives when work touches the
    subject, not preemptively
  - done: every row in the `CLAUDE.md` table resolves to a file matching its keywords

## NICE

- [ ] **`N3-coverage-number`** — not a catalog id; recorded because it is the one part of the
  test axis that could not be measured. evidence: `php -m → neither xdebug nor pcov`;
  `composer test:coverage` exists but has no driver to run against. action: the PHP 8.4 leg of
  `checks` installs `pcov` and runs `composer test:coverage` — the step is written but has never
  executed, since no local driver exists to rehearse it. done: a coverage figure appears in a
  CI run

## n/a

- `M5-secrets` — a library holds no `.env`; no credential is tracked (`git grep` clean).
  Passed, see Closed.
- `M8-sentry` — error monitoring belongs to the host application, not to a library.
- `S2-eslint-rules` — no JavaScript in the repo.
- `S4-deploy-shape`, `S5-make-update`, `S6-php-aligned`, `S7-semver`, `S8-image-tags` — the
  package is consumed by Composer version constraint; there is no image, no VPS and no deploy.
- `S10-composer-img` — no `docker-compose.yml` and no Sail runtime to align a tag against.
- `S11-db-disposable` — the package owns no schema and ships no migration. The
  `workbench/database/migrations/` file is test scaffolding, rebuilt per test run.
- `N4-run-skill` — a library has no app to drive.

## Closed

- [x] **`M2-branches`** — evidence: `git branch -a → master, feat/MotorDePlanilhas,
  remotes/origin/master`; no `Sandbox`. **closed 2026-09-03** — the variant is deliberate and is
  documented in `CLAUDE.md` § Git overrides: a package is consumed by Composer version
  constraint, not deployed, so there is no QA environment for a `Sandbox` branch to feed. User
  confirmed 2026-09-03. The rest of the git model (`feat/`/`fix/` off `master`, `--no-ff`,
  upstream rule) applies unchanged.

- [x] **`M3-upstream`** — evidence: `git for-each-ref → feat/MotorDePlanilhas ->
  origin/feat/MotorDePlanilhas`, `master -> origin/master`. **closed 2026-09-03.**

- [x] **`M4-commits`** — evidence: three conforming commits on `feat/MotorDePlanilhas`
  (`(MINOR) refactor(planilhas): …`, `docs(planilhas): …`, `ci(planilhas): …`). The single
  pre-existing `first commit` stays as it is: published history is never rewritten.
  **closed 2026-09-03.**

- [x] **`M7-gates`** — evidence: `.github/workflows/checks.yml`, triggered by `pull_request`
  into `master` and by `push` to `master`, running `pint --test` → `composer test` over a PHP
  **8.2 / 8.3 / 8.4** matrix. **closed 2026-09-03.**
  - **One pipeline, not two** — the "both pipelines" wording of the catalog assumes a `Sandbox`
    deploy this repo deliberately does not have (`M2`, Closed). `dusk` is `n/a` — package, no
    UI. There is no build or deploy job either: the package ships through a Composer version
    constraint, so `checks` is the whole gate and nothing `needs:` it.
  - **The matrix is load-bearing, not thoroughness theatre.** `openspout` resolves to v4 on 8.2
    and v5 on 8.3+, and the majors are not source-compatible (`CLAUDE.md` § Workflow). A single
    leg would let `^4.28|^5.3` rot.
  - No MySQL service, no Node: sqlite in memory, no JavaScript in the repo.

- [x] **`M5-secrets`** — evidence: `no .env, no .env.example needed (library)`; `git grep` for
  tracked credentials clean; nothing under `config/sheets.php` holds a secret — the keys are
  `env()` reads with safe defaults. **closed 2026-09-03** — conforms as-is.

- [x] **`M6-verify`** — evidence: `composer test → OK (73 tests, 187 assertions)`;
  `composer lint → {"tool":"pint","result":"passed"}`. `phpunit.xml`, `testbench.yaml` and a
  `workbench/` application were added, following the layout of `luminix/media-gallery` and
  `luminix/laravel-permission-integration`. **closed 2026-09-03.**

- [x] **`M9-supported-runtime`** — evidence: `composer.json → "php": "^8.2"`,
  `"laravel/framework": "^11.0|^12.0|^13.0"`, matching what `luminix/backend` and
  `luminix/frontend` declare. Was `^8.1` / `^10.0|^11.0`. **closed 2026-09-03.**

- [x] **`S3-pint`** — evidence: `pint.json` (`{"preset": "laravel"}`), `laravel/pint` in
  `require-dev`, `composer lint` / `composer format` scripts, whole tree clean.
  **closed 2026-09-03.**

- [x] **`S9-test-layout`** — evidence: `workbench/app/Tests/Feature/` — `ExportTest`,
  `ImportTest`, `RouteTest`, `RoutesDisabledTest`, `StreamingTest`, `ManifestTest`,
  `PermissionTest`, `MakeCommandsTest` — plus `workbench/app/Tests/Unit/` for the support
  classes, over Testbench fixture models. `withoutVite()` is `n/a` (no UI).
  **closed 2026-09-03.**

- [x] **`M10-readme`** — evidence: `README.md`, in português, sections 1–5 in the canonical
  order; requisitos lead with `/arandu`; the install section was executed end to end
  (`composer install` → `composer test` green). H1 **`# Luminix Sheets`** confirmed by the user
  2026-09-03, matching the sibling convention. **closed 2026-09-03.**

- [x] **`S12-readme-badges`** — evidence: four badges, each read from `composer.json`
  (`laravel/framework` → 11 | 12 | 13, `php` → 8.2+, `luminix/backend` → 1.x, `license` → MIT).
  **closed 2026-09-03.**

- [x] **`N1-editorconfig`** / **`N2-gitattributes`** / **`.gitignore`** — all three present.
  `LICENSE` (MIT, the license `composer.json` declares) added alongside them.
  **closed 2026-09-03.**

- [x] **Engine defects found during the audit** — product bugs rather than scaffold gaps, kept
  here because the audit report is where they were raised. All fixed on `feat/MotorDePlanilhas`
  and each covered by a test. **closed 2026-09-03.**
  - 🔴 **No access control on either route.** `luminix/backend`'s permission map has no
    `import` / `export` key, so `inferRequestParameters()` returned `null` — skipping both the
    `Gate` and the `allowed()` row scope. Fixed: the package owns the mapping
    (`config/sheets.php → permissions`, read through
    `LuminixSheetsServiceProvider::permissionFor()`). Tests: `PermissionTest`, plus the 401
    cases in `ExportTest` / `ImportTest`.
  - 🔴 **The export held the whole table in memory** and `streamDownload()` streamed nothing.
    Fixed: `->lazy(chunk_size)` into an OpenSpout writer, file closed before the response
    starts, temp file removed on both paths. Tests: `StreamingTest` (query count per chunk,
    bounded memory over 5.000 rows), `ExportTest::test_a_failure_mid_file_surfaces_as_an_error_and_cleans_up`.
  - 🟠 **`phpoffice/phpspreadsheet` replaced by `openspout/openspout`** — the library locks up on
    large sheets, which is why AppBase moved off it. `SpreadsheetWriter` / `SpreadsheetReader`
    wrap OpenSpout and shim the v4/v5 API split. `.xls` was dropped: OpenSpout cannot read it and
    `mimes:xlsx` is already the precedent in `base-de-dados-api`
    (`PlayerController:25`, `ClassroomController:19`). Tests: `SpreadsheetWriterTest`,
    `SpreadsheetReaderTest`, run green against **both** majors — PHP 8.2 / openspout v4.28.5 and
    PHP 8.4 / v5.11.3. The v5 run is what exposed the `Row` constructor break; PHP 8.3 could not
    be exercised locally (no `pdo_sqlite`), which is one of the reasons the CI matrix exists.
  - 🟠 **The export ignored the listing's filters and ordering.** Fixed: the macro calls
    `$class::luminixQuery($request, $permission)`, the same entry point the index uses. Tests:
    the `q` / `where` / `order_by` / `allowed` cases in `ExportTest`.
  - 🟠 **Routes sat under a singular prefix and raced `show`.** Fixed: registered through
    `RouteGenerator::reducer('modelRoutes', …)` in `register()`, spread *before* the default
    routes, so they inherit the plural prefix, the middleware and the `luminix.{alias}.{action}`
    naming. Tests: `RouteTest` (including the ordering against `show` / `update`),
    `RoutesDisabledTest`.
  - 🟠 **The manifest flags never shipped** — `extendManifest()` guarded on a `Finder::extend()`
    that does not exist. Fixed: `ManifestService::reducer('modelManifest', …)`, guarded by
    `class_exists` so `luminix/frontend` stays optional. Test: `ManifestTest`.
  - 🟠 **`HiddenColumns` documented an API that could not work** — a protected `$sheetsHidden`
    read from outside the class fell through `Model::__get()` and became `[]`. Fixed with
    `ReflectionProperty`. Test: `HiddenColumnsTest`, over a fixture whose hidden column is *only*
    in `$sheetsHidden`, so the fallback cannot mask the bug.
  - 🟡 The smaller ones: `columns()` / `allowedColumns()` are now honoured (the latter enforced
    by the engine, not just the default handler); `afterExport()` fires once the file exists;
    `export.max_rows` and `export.default_format` are read; the route scan per request is gone
    (routes live in the backend's own cacheable file); `ImportRequest` resolves after the gate;
    attributes resolve through inheritance and `IS_INSTANCEOF`; the empty
    `src/Http/Controller.php` was deleted; `"*"` constraints replaced with real ones.

## Report-only — not actionable by /arandu

- **`minimum-stability: dev` is still declared.** `luminix/backend` publishes stable tags
  (1.1.3 installs), so it may no longer be needed — but `arandu/reducible` and the rest of the
  Luminix tree were not checked one by one. Removing it is a small scoped change with a real
  chance of breaking resolution for consumers; verify with a clean `composer update` on each
  supported Laravel major before touching it.
